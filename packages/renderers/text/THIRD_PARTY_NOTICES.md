# Third-party notices

`@file-viewer/renderer-text` packages the browser standalone build and selected parser plugins from **Prettier** for the opt-in `options.text.prettyPrint` display path.

- Project: Prettier
- License: MIT
- Copyright: James Long and contributors

The complete Prettier license text is copied into `dist/prettier/LICENSE` during the package build. The formatter is self-hosted with the renderer and does not contact a CDN or online formatting service.
